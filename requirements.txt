# Core ML platform — https://pypi.org/project/sitaka-api/
sitaka-api[deploy,parquet]>=0.1.0

# Pin sklearn so shipped model.joblib loads without version warnings
scikit-learn==1.7.2

# Analytics application
streamlit>=1.33
plotly>=5.20
pandas>=2.0
numpy>=1.24
scipy>=1.11
pyyaml>=6.0
jinja2>=3.1

# Optional: Kaggle dataset download (local dev only)
kaggle>=1.6
